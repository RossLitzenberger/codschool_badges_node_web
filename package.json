{
  "name": "soup-to-bits-web-server",
  "version": "0.0.0",
  "description": "soup to bits web server",
  "main": "app.js",
  "scripts": {
  "test": "mocha tests"
  },
  "dependencies": {
  "express": "^3.5.1",
  "redis": "^0.10.1",
  "request": "^2.34.0",
  "socket.io": "^1.2.0"
  },
  "engines": {
  "node": "0.10.x"
  }
}
